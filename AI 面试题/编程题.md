blockreduce  
rmsnorm
MHA
MGA
MLA
手撕，算sqrt()，要写两种方法，牛顿迭代和二分查找
快排
堆排
归并排序
LRU
生产者-消费者
按指定概率采样（如[(1,0.5),(2,0.1),(3,0.4)]）
井字棋判胜：读取 txt 井字棋（3×3）状态，判断当前玩家是否获胜。  
交叉熵损失：解释原理并手写实现。  
线性回归 SGD：推导更新公式并手写实现。  
手写一个简单的Attention计算
写reduce_sum的算子
代码实现online softmax
手撕内存池（要求支持类似new Foo[], delete []功能)
c++如何比较两个float是否相等
hamming weight
k-coverage intervals
支持torch broadcast的4D tensor的elementwise mul
A: (1, 256),  B: (256, 128),  C: (128, 256) ，计算 (A * B) * C
Embedding Sparse Feature Pooling：A是 100 万个离散 ID（0~999），B是 100 万个float，计算个长度为1000的float数组C，C[i] = \sum_{j s.t. A[j] = i} B[j]  
layer norm
大矩阵reduce规约求和
softmax
online-softmax
linear atten手撕
moe手撕
实现二维矩阵转置，逐步优化
Flash Attention v1
flow matching model采样
CUDA编程：在 uint8 数组中找第 K 大的值
large array reduce sum 实现（shared memory归约 + 分层kernel设计）
cuda算子:前缀和。实现一个base的，讲优化方法
cuda算子gemm:实现一个base的，讲优化方法
行TOP1 CUDA实现（shared memory归约 + 多行优化）
二维大矩阵reduce，（1000000，128）矩阵reduce到（1，128）
