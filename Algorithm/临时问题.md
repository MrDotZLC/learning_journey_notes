1. 传入并修改裸指针，为什么要传入指针地址，即T**？
   答：C语言参数传递是**值传递**，T\*仅代表T指针的地址值，故函数会创建一个值副本，没有修改传入指针。如果要修改函数外的变量，需要传入T\*\*或智能指针引用
   
2. struct和class的区别
   答：**struct** 和 **class** 唯一的语言层面区别是：
	- **struct** 默认成员和继承是 `public`。
	- **class** 默认成员和继承是 `private`。

3. 栈和堆的区别？
   答：