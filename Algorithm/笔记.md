1. 数组
2. 链表
3. 哈希表
4. 字符串
5. 双指针法
6. 栈与队列
7. 二叉树
8. 回溯算法
9. 贪心算法
10. 动态规划
11. 单调栈
12. 图论
13. 其他

# 原始数组
![[Pasted image 20250711022215.png]]

## 知识
### 基础概念
1. int arr\[3\] = {10, 20, 30};
2. 数组是存放在连续内存空间上的相同类型数据的集合。
3. 数组下标都是从0开始的。
4. 数组内存空间的地址是连续的，增删可能会移动其他元素。
5. 数组元素只能覆盖，不能删！
6. 不能释放单一元素，只能全部释放

### 数组的存储
1. 原始二维数组是一维连续存储
2. `array`本质上就是一个封装了原始数组的结构体，不涉及动态内存分配，本身是连续存储的，其二维数组也是一维连续存储
3. `vector`的每一行独立存储，其二维数组非连续
4. `int**` 动态二维指针，不连续，每一行指向单独分配的数组，位置随机
5. `std::vector<std::array<int, 4>>`中每个`array`是连续的，整体也是连续

### 数组为什么不能释放单个元素？
- 原因 1：连续内存 + 统一分配，
  数组的所有元素是一次性整体分配的（无论在栈还是堆上）。
  操作系统或运行时不会允许你释放这片内存的一部分。
- 原因 **2**：释放机制只认“起始地址”
  `free(ptr)` 只能用于释放当初 `malloc` 返回的那个原始地址。
  指向数组中间某个元素的地址是非法释放行为。

### 释放部分内容的替代方法
  1. 用结构记录“有效元素”范围
```
int* arr = malloc(100 * sizeof(int));
int size = 100;
int valid_size = 60;  // 实际只用前 60 个元素
```
2. 拆成多个小块
```
int* blocks[10];
for (int i = 0; i < 10; ++i)
    blocks[i] = malloc(10 * sizeof(int));

// 想释放第5块
free(blocks[5]);
```
### ==*拓展：栈与堆的对比==

| 特性     | 栈（Stack）    | 堆（Heap）             |
| ------ | ----------- | ------------------- |
| 分配方式   | 编译器自动分配     | 程序员手动分配（malloc/new） |
| 释放方式   | 自动释放（函数结束时） | 手动释放（free/delete）   |
| 分配速度   | 非常快         | 相对较慢                |
| 大小限制   | 有限（几MB）     | 取决于物理内存             |
| 地址空间   | 高地址向低地址增长   | 低地址向高地址增长           |
| 生命周期   | 与作用域绑定      | 控制权在程序员             |
| 可否动态扩展 | ❌ 不行        | ✅ 可以动态申请更大空间        |

## 相关题目
1. 二分查找：34 35 69 367 704
2. 移除元素：26 27 283 977
3. 有序数组的平方：977
4. 长度最小的子数组：76 209 904 
5. 螺旋矩阵：54 59 J29
6. 区间和：58 
7. 开发商购买土地：44

# 链表
## 知识

```
// 单链表
struct ListNode {
    int val;  // 节点上存储的元素
    ListNode *next;  // 指向下一个节点的指针
    ListNode(int x) : val(x), next(NULL) {}  // 节点的构造函数
};

ListNode* head = new ListNode();
ListNode* head = new ListNode(5);
```

类型：单链表、双链表、循环链表

| 对比项   | 数组           | 链表              |
| ----- | ------------ | --------------- |
| 内存连续性 | ✅ 是          | ❌ 否             |
| 随机访问  | ✅ 快 (`O(1)`) | ❌ 慢 (`O(n)`)    |
| 插入删除  | ❌ 慢 (`O(n)`) | ✅ 快（`O(1)` 若定位） |
| 内存开销  | ✅ 低          | ❌ 高（需指针）        |
| 空间利用率 | ✅ 高          | ❌ 低（碎片+指针）      |
| 实现复杂度 | ✅ 简单         | ❌ 稍复杂（需指针）      |
| 扩容能力  | ❌ 需要重分配      | ✅ 不需重分配         |

## 相关题目

1. 移除链表元素：203
2. 设计链表：707
3. 反转链表：206
4. 两两交换链表中的节点：24
5. 删除链表的倒数第N个节点：19
6. 链表相交：160
7. 环形链表II：142